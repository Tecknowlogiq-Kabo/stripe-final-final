apiVersion: 1

providers:
  - name: 'Stripe API'
    orgId: 1
    folder: 'Stripe API'
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
